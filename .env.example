# ---- Extension (Plasmo) ----
# Only PLASMO_PUBLIC_* vars are exposed to the extension bundle.
PLASMO_PUBLIC_BACKEND_API_URL=http://localhost:8000

# ---- Backend (FastAPI) ----
OPENAI_API_KEY=
# Model used to convert raw recipe ingredients into Instacart line items.
OPENAI_MODEL=gpt-4o-2024-08-06
# Optional. Leave blank to use the model's default temperature (required for
# models that reject a custom temperature, e.g. reasoning models). Set to e.g.
# 0 for deterministic output on models that support it.
OPENAI_TEMPERATURE=

# Instacart Developer Platform.
# Dev keys (prefix "keys.") use the dev server; prod keys use https://connect.instacart.com
INSTACART_SERVER=https://connect.dev.instacart.tools
INSTACART_API_KEY=
# Optional affiliate/UTM suffix appended to the generated products link.
INSTACART_PARTNER_URL=
# Outbound Instacart request timeout in seconds.
INSTACART_TIMEOUT=15

# Browser origins allowed to call the API (comma-separated), or "*" for any.
# To lock the deployed API to just the published extension, set it to the
# extension's origin:
#   ALLOWED_ORIGINS=chrome-extension://fbnbcmkopjplpopnjmohjfnphlaaldph
ALLOWED_ORIGINS=*
